import { Field } from '../../../../fields/config/types';
declare const getInitialColumnState: (fields: Field[], useAsTitle: string, defaultColumns: string[]) => {
    columns: string[];
};
export default getInitialColumnState;
