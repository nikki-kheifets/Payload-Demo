import React from 'react';
import Select from 'react-select';
import Chevron from '../../icons/Chevron';
import './index.scss';
const ReactSelect = (props) => {
    const { showError = false, options, onChange, value, disabled = false, } = props;
    const classes = [
        'react-select',
        showError && 'react-select--error',
    ].filter(Boolean).join(' ');
    return (React.createElement(Select, { ...props, value: value, onChange: onChange, disabled: disabled ? 'disabled' : undefined, components: { DropdownIndicator: Chevron }, className: classes, classNamePrefix: "rs", options: options }));
};
export default ReactSelect;
