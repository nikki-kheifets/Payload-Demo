import { SanitizedCollectionConfig, CollectionConfig } from './types';
declare const sanitizeCollection: (collections: CollectionConfig[], collection: CollectionConfig) => SanitizedCollectionConfig;
export default sanitizeCollection;
