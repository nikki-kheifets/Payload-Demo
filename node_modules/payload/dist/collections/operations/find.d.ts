import { Where } from '../../types';
import { PayloadRequest } from '../../express/types';
import { Collection, PaginatedDocs } from '../config/types';
export declare type Arguments = {
    collection: Collection;
    where?: Where;
    page?: number;
    limit?: number;
    sort?: string;
    depth?: number;
    req?: PayloadRequest;
    overrideAccess?: boolean;
    showHiddenFields?: boolean;
};
declare function find(incomingArgs: Arguments): Promise<PaginatedDocs>;
export default find;
