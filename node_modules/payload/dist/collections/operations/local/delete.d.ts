import { Document } from '../../../types';
export declare type Options = {
    collection: string;
    id: string;
    depth?: number;
    locale?: string;
    fallbackLocale?: string;
    user?: Document;
    overrideAccess?: boolean;
    showHiddenFields?: boolean;
};
export default function localDelete(options: Options): Promise<Document>;
