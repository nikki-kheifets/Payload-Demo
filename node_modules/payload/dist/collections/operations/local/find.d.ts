import { PaginatedDocs } from '../../config/types';
import { Document, Where } from '../../../types';
export declare type Options = {
    collection: string;
    depth?: number;
    page?: number;
    limit?: number;
    locale?: string;
    fallbackLocale?: string;
    user?: Document;
    overrideAccess?: boolean;
    showHiddenFields?: boolean;
    sort?: string;
    where?: Where;
};
export default function find(options: Options): Promise<PaginatedDocs>;
