import { Payload } from '..';
import { Field, HookName } from './config/types';
import { Operation } from '../types';
import { PayloadRequest } from '../express/types';
declare type Arguments = {
    data: Record<string, unknown>;
    fullData: Record<string, unknown>;
    originalDoc: Record<string, unknown>;
    field: Field;
    operation: Operation;
    overrideAccess: boolean;
    req: PayloadRequest;
    id: string;
    relationshipPopulations: (() => Promise<void>)[];
    depth: number;
    currentDepth: number;
    hook: HookName;
    payload: Payload;
};
declare const accessPromise: ({ data, fullData, originalDoc, field, operation, overrideAccess, req, id, relationshipPopulations, depth, currentDepth, hook, payload, }: Arguments) => Promise<void>;
export default accessPromise;
