import { PayloadRequest } from '../express/types';
import { Operation } from '../types';
import { Field, HookName } from './config/types';
declare type Arguments = {
    data: Record<string, unknown>;
    field: Field;
    hook: HookName;
    req: PayloadRequest;
    operation: Operation;
    fullOriginalDoc: Record<string, unknown>;
    fullData: Record<string, unknown>;
};
declare const hookPromise: ({ data, field, hook, req, operation, fullOriginalDoc, fullData, }: Arguments) => Promise<void>;
export default hookPromise;
