import { PayloadRequest } from '../express/types';
import { Field } from './config/types';
import { Payload } from '..';
declare type PromiseArgs = {
    data: Record<string, any>;
    field: Field;
    depth: number;
    currentDepth: number;
    req: PayloadRequest;
    overrideAccess: boolean;
    payload: Payload;
};
declare const relationshipPopulationPromise: ({ data, field, depth, currentDepth, req, overrideAccess, payload, }: PromiseArgs) => () => Promise<void>;
export default relationshipPopulationPromise;
