import { Payload } from '..';
import { RichTextField } from './config/types';
import { PayloadRequest } from '../express/types';
declare type Arguments = {
    data: unknown;
    overrideAccess?: boolean;
    depth: number;
    currentDepth?: number;
    payload: Payload;
    field: RichTextField;
    req: PayloadRequest;
};
declare const richTextRelationshipPromise: ({ req, data, payload, overrideAccess, depth, currentDepth, field, }: Arguments) => () => Promise<void>;
export default richTextRelationshipPromise;
