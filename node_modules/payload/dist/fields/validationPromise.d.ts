import { Field, HookName } from './config/types';
declare type Arguments = {
    hook: HookName;
    field: Field;
    path: string;
    errors: {
        message: string;
        field: string;
    }[];
    newData: Record<string, unknown>;
    existingData: Record<string, unknown>;
    skipValidation?: boolean;
};
declare const validationPromise: ({ errors, hook, newData, existingData, field, path, skipValidation, }: Arguments) => Promise<string | boolean>;
export default validationPromise;
