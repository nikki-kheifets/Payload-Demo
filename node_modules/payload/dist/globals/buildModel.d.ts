import mongoose from 'mongoose';
import { SanitizedConfig } from '../config/types';
declare const buildModel: (config: SanitizedConfig) => mongoose.PaginateModel<any> | null;
export default buildModel;
